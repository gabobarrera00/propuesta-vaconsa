.vercel
node_modules/
.worktrees/

# build.py output (regenerated by `python build.py` / `npm run build`) —
# not pages/inicio.html, the source; just the generated root index.html
# and the per-section output folders it writes via output_path()
/index.html
/catalogo/
/cotizar/
/desarrolladores/
/donde-estamos/
/fichas-tecnicas/
/nosotros/
/portal-clientes/
/respaldo-tecnico/
/responsabilidad/
/servicios/

# leftover from local `python -m http.server` verification runs
server.log
