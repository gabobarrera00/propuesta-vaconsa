{
  "name": "propuesta-vaconsa",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "python3 build.py || python build.py",
    "start": "npm run build && serve ."
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
